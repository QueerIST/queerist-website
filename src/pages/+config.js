import vikeReact from 'vike-react/config'

// https://vike.dev/config
export default {
  // Setting to toggle SSR
  ssr: false,
  // https://vike.dev/head
  favicon: '/vite.svg',
  extends: [vikeReact]
}
